let git = {
  1: "git config --global user.name 'Martin Suarez' ....set a name",
  2: "git config user.name ....just asking",
  3: "git config user.email",
  4: "git config --global user.email colt@email.com",
  5: "ls -a",
  6: "cd",
  7: "start . || open .",
  8: "pwd print working directory",
  9: "cd change directory",
  10: "cd ..",
  11: "touch one.js",
  12: "mkdir folderOne",
  13: "rm one.js",
  14: "rm -rf folderOne",
  15: "git status",
  16: "git init",
  17: ".git folder contains all the git history",
  18: "git add .",
  19: "",
  20: "git stash save 'my-mesagge'",
  21: "git stash list",
  22: "git stash apply stash@{0}",
  23: "git stash pop",
  24: "",
  25: "",
  26: "",
  27: "",
  28: "",
};

// git commit -a -m 'made a change'
// git branch <branch name>
// git checkout testing //switch branch
// git checkout -b <newbranchname> //to create a new branch and want to switch to that new branch at the same
// git switch testing-branch //switch branch
// git switch -c new-branch //to create a new branch and want to switch to that new branch at the same -c stands for create
// git branch -d hotfix //delete a branch

//git log ... to see the list of all commits  //git log --oneline
//git reset --hard HEAD^ .... undo last commit ... opposite to git commit -m "myMessagge"

// ssh vs http
// git commit -a -m " Commit Message"
// To undo git add before a commit, run git reset <file> or git reset to unstage all changes.
// https://www.freecodecamp.org/news/how-to-undo-a-git-add/

// git config -l //all the info of my branch
// .gitignore
// git commit -am "my-message"
// git merge
// git log
// git push origin (myBranch)
//git log --oneline --decorate --all --graph

//----------------------------------------------------------fer herrera----------------------//
//----------------------------------------------------------fer herrera----------------------//
//----------------------------------------------------------fer herrera----------------------//
// gist
// activitusBar download
// terminal
// git --version(double dash means we're gonna type an entire word)
// git help
// :q
// git --help config
// git config --global user.name "Fer her"
// git config --global user.email "Ferher@gmail.com"
// git config --global -e
// :wq!
// git init
// git add .
// git reset myFileName.js
// (soy programadora git reset)
// git checkout -- . //goes to last commmit
// video 12
